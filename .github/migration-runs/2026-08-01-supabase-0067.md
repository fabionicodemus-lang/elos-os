# Supabase 0067 · Staging canônica do Koper

Execução controlada pela PR operacional intitulada exatamente `[RUN] Supabase 0067`.

## Migration

`20260801_0067_koper_staging_records.sql`

## Resultado esperado

- tabela `public.koper_staging_records` criada;
- unicidade por `(company_id, source, entity, koper_id)`;
- RLS ativa com leitura restrita aos membros da empresa;
- nenhum grant ou policy de escrita para usuários autenticados;
- payload normalizado preparado para idempotência e auditoria.

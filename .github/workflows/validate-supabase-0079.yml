name: Validate Supabase 0079

on:
  pull_request:
    branches: [main]
    paths:
      - "supabase/migrations/20260804_0079_budget_item_compositions.sql"
      - ".github/tests/budget-item-compositions-0079-v2.sql"
      - ".github/workflows/validate-supabase-0079.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: elos_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d elos_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - name: Executar migration e cenários de isolamento
        env:
          PGPASSWORD: postgres
        run: psql -h 127.0.0.1 -U postgres -d elos_test -v ON_ERROR_STOP=1 -f .github/tests/budget-item-compositions-0079-v2.sql
